all:
    children:
        syslog:
           hosts:
                srv-syslog:
                    ansible_ssh_user: ansible
        web:
            hosts:
                web1:
                    ansible_ssh_user: ansible
                web2:
                    ansible_ssh_user: ansible
        bdd:
            hosts:
                bddmaster:
                    ansible_ssh_user: ansible
                bddslave:
                    ansible_ssh_user: ansible